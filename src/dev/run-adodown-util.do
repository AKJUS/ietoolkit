
  reproot , project(ietoolkit) roots(clone) prefix(ietk_)
  
  ad_sthlp , adfolder("${ietk_clone}")
  
  //ad_update , adfolder("${ietk_clone}") pkgname("ietoolkit") newpkgversion(minor) 
  
  //ad_publish, adf("${ietk_clone}") ssczip
